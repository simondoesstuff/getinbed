# Ensure cmake is available for flate2/zlib-ng in cross-compilation Docker images.
[build.env]
passthrough = ["CARGO_TERM_COLOR"]

[target.aarch64-unknown-linux-gnu]
pre-build = ["DEBIAN_FRONTEND=noninteractive apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y cmake"]
